Note!
- this is a project where i create a Naive Bayes model to detect real or fake jobs based on the text given.
- you can change the dataset (e.g: sentiment classification) by just changing the dataset path.
